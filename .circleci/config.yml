version: 2
jobs:
  build:
    docker:
      - image: circleci/node:13.2.0
    steps:
      - checkout
      - run:
          name: Installing dependencies
          command: npm install
  test:
    docker:
      - image: circleci/node:13.2.0
    steps:
      - run:
          name: Running tests
          command: npm test

workflows:
  version: 2
  main:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
      - test:
          requires:
            - build
          filters:
            tags:
              only: /.*/